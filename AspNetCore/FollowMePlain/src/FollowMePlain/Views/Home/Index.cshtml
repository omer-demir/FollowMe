@{
    Layout = "_Layout";
}
<style>
    iframe {
        width: 100%;
        height: 300px;
    }
</style>

@*<div class="row">
    <div class="col-lg-12" ng-show="selectedBlogItem">
        <h1>{{selectedBlogItem.title}}</h1>
        <p>{{selectedBlogItem.shortDescription}}</p>
        <iframe ng-src="{{selectedBlogItem.trustedResourceUrl}}" frameborder="0"></iframe>
    </div>

    <div class="col-md-12">
        <div ng-repeat="post in selectedBlogItemPosts">
            <a href="{{post.url}}">{{post.title}}</a>
            <br/>
        </div>
    </div>
</div>*@


<div class="row">
    <div class="col-lg-12">
        <h1>{{latestItemRssFeed.title.text}}</h1>
        <p>{{latestItemRssFeed.pubDate.text}}</p>
        <p ng-bind-html="latestItemRssFeed.description.text"></p>
    </div>
    
</div>